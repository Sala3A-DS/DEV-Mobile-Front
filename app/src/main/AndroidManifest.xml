<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permite o app acessar internet/API -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Agend">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".CadastroActivity" />
        <activity android:name=".AgendamentoActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".CadastrarSalaActivity" />
        <activity android:name=".DiretorHomeActivity" />

        <!-- Corrigido: faltava o ponto antes do nome -->
        <activity android:name=".EsqueciSenhaActivity" />

    </application>

</manifest>